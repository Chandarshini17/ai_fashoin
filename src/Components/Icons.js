import React from "react"; import Instagram from "../images/icon1.png"; import Facebook from "../images/icon2.png"; import Youtube from "../images/icon3.png"; import Notion from "../images/icon4.png"; import Figma from "../images/icon5.png"; const Icons = () => { const iconUrls = [Instagram, Facebook, Youtube, Figma, Notion]; const containerStyle = { display: "flex", justifyContent: "center", alignItems: "center", gap: "20px", padding: "20px", backgroundColor: "#f9f9f9", borderRadius: "10px", boxShadow: "0 0 10px rgba(0, 0, 0, 0.1)" }; const iconStyle = { width: "60px", height: "60px", borderRadius: "10px", transition: "transform 0.3s ease, box-shadow 0.3s ease", cursor: "pointer" }; const handleHover = (e) => { e.target.style.transform = "scale(1.1)"; e.target.style.boxShadow = "0 5px 15px rgba(0, 0, 0, 0.2)"; }; const handleLeave = (e) => { e.target.style.transform = "scale(1)"; e.target.style.boxShadow = "none"; }; return <div style={containerStyle}>{iconUrls.map((url, index) => (<img key={index} src={url} alt={`Icon ${index}`} style={iconStyle} onMouseEnter={handleHover} onMouseLeave={handleLeave} />))}</div>; }; export default Icons;
